[package]
name = "smartassist-cli"
version = "0.1.0"
edition = "2021"
description = "Command-line interface for SmartAssist"
license = "MIT"

[[bin]]
name = "smartassist"
path = "src/main.rs"

[dependencies]
smartassist-core = { path = "../smartassist-core" }
smartassist-agent = { path = "../smartassist-agent" }
smartassist-gateway = { path = "../smartassist-gateway" }
smartassist-channels = { path = "../smartassist-channels" }
smartassist-providers = { path = "../smartassist-providers", features = ["anthropic", "openai", "google", "ollama", "deepseek", "moonshot", "openrouter", "qwen", "zhipu"] }
smartassist-secrets = { path = "../smartassist-secrets" }
smartassist-plugin-sdk = { path = "../smartassist-plugin-sdk" }

# Async runtime
tokio = { version = "1.35", features = ["full"] }

# CLI framework
clap = { version = "4.4", features = ["derive", "env"] }

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Secret management
secrecy = "0.10"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Terminal
console = "0.16"
indicatif = "0.18"

# Terminal UI
ratatui = "0.30"
unicode-width = "0.2"
unicode-segmentation = "1.11"
rpassword = "7.3"
rustyline = "18.0"
rustyline-derive = "0.12"
termimad = "0.35"
crossterm = "0.29"
ctrlc = "3.4"

# HTTP client
reqwest = { version = "0.13", features = ["json"] }

# Async streams
futures = "0.3"
async-stream = "0.3"

# Config
directories = "6.0"

[dev-dependencies]
tempfile = "3"

[features]
default = []
